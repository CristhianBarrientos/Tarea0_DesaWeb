* {
    font-family: cursive;
}

body {
    margin: 0;
    background-color: #f4f1ee; 
}

/* una sola configuracion para todos lo sinputs */
input {
    border-radius: 20px;
    background-color: #4a3728; 
    font-style: italic;
    width: auto; /* Largo */
    height: 2vw; /* alto */
    margin: 2vh 2vh 0;
    padding: 15px;
    color: #f5f3f0; 
    box-shadow: 6px 7px 8px #d4c4b0; 
}

input:hover {
    transform: scale(1.03);
    transition: transform 0.2s;
}

input::placeholder {
    color: rgba(245, 243, 240, 0.689);  
    font-style: bold;
}

/* Estilos para delimitar el resize y para poner bonito el text area */
#txtMensaje {
    border-radius: 10px;
    margin-top: 2vh;
    padding: 10px;
    width: 95%;
    align-self: center;
    background-color: #4a3728; 
    color: #f5f3f0; 
    font-style: bold;
    box-shadow: 6px 7px 8px #d4c4b0;  
    max-width: 95%;
}

textarea::placeholder {
    color: rgba(245, 243, 240, 0.689);  
}

textarea:hover {
    transform: scale(1.03);
    transition: transform 0.2s;
}

/* estilos para el bot[on de enviar */
#btnEnviar {
    margin-top: 10px;
    border-radius: 5px;
    width: 97%;
    height: 4vw;
    padding: 10px;
    align-self: center;
    color: #f5f3f0; 
    font-size: 3vh;
    background-color: #6b4e3d;  
    box-shadow: 6px 7px 8px #d4c4b0;  
}

#btnEnviar:hover {
    background-color: #6b4e3dc0;  
    transform: scale(1.03);
    transition: transform 0.2s ease;
}

#btnEnviar:active {
    background-color: #6b4e3d;  
    transform: scale(1);
    transition: transform 0.1s ease;
}


/* Contenedor de lenguajes de programación */
.lenguajesProgra {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #e8ddd4;  
    padding: 20px;
    border-radius: 10px;
    margin: 1vw;
}

/* Contenedor de información */
.info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #e8ddd4;  
    margin: 1vw;
    padding: 15px;
    border-radius: 10px;
}

/* Contenedor de tabla de datos */
.tablaDatos {
    display: flex;
    justify-content: center;
    background-color: #e8ddd4;  
    margin: 1vw;
    padding: 15px;
    border-radius: 10px;
}

/* Contenedor de inputs de datos personales */
.inputsDatosPersonales {
    display: flex;
    justify-content: center;
    background-color: #e8ddd4; 
    margin: 1vw;
    padding: 15px;
    border-radius: 10px;
}

.inputsDatosPersonales h1 {
    align-self: center;
    font-style: italic;
    color: #5d4037; 
    /* background-color: #4a3728; */
    /* box-shadow: 3px 10px 25px rgba(0, 0, 0, 0.1); */

}

/* esto es para centrar las cosas del header */
.contenedorCentrado {
    flex: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.contenedorCentrado h2 {
    margin: 0;
    font-size: 20px;
    color: #5d4037; 
}

/* para centrar los inputs nada mas */
.inputsCentrados {
    display: flex;
    flex-direction: column;
    width: 60%;
}



/* estilo para la imagen que sale hasta arriba de la pag */
.persona {
    width: 80px;
    height: 80px;
    background-color: #d4c4b0; 
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 36px;
    color: #f5f3f0; 
    margin-bottom: 10px;
}

/* Tabla */
.tabla {
    width: 100%;
    border-collapse: collapse;
    background-color: #f5f3f0; 
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 25px rgba(74, 55, 40, 0.1); 
}

.tabla:hover {
    transform: scale(1.03);
    transition: transform 0.2s ease;
}

/* se pueden colocar estrilos a un th, entonces se coloca un color adecuao al ambiente para el titulo */
.tabla th {
    background: #5d4037; 
    color: #f5f3f0; 
    font-weight: 600;
    font-size: 18px;
    padding: 20px 15px;
    text-align: center;
    letter-spacing: 1px;
    text-transform: uppercase;
    border: none;
}

/* las celdas tambien tienen para estilos, entonces se coloca ahora si, uniformemente mitad y mitad las columnas para que se vea mejor, se atenuan los bordes para que no se vea puro excel xd y por ultimo se coloca el iunterior de un color que combine */
.tabla td {
    width: 50%;
    padding: 15px 20px;
    text-align: center;
    font-size: 16px;
    font-weight: 500;
    color: #4a3728; 
    background-color: #d4c4b0; 
    font-style: bold;
    /* border: none; */
    border-right: 1px solid #c4b59ff0; 
    border-bottom: 1px solid #c4b59ff5; 
}

.menu {
    margin-top: 5px;
}

.menu a {
    margin-top: 5px;
    text-decoration: none;
    color: #8d6e63; 
    font-size: 14px;
}

.menu a:hover {
    text-decoration: underline;
    color: #4a3728; 
}


/* Lista principal (Paginas Web y Aplicaciones*/
.listaPrinc {
    list-style-type: square;
}

/* Lista de información (info de las paginas y de las aplicaciones , segundo bloque) */
.listaInfo {
    list-style-type: circle;
}